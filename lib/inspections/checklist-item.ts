/** Shared checklist row shape for US (NFPA) and UK (BS) template packs. */
export type ChecklistTemplateItem = {
  label: string;
  /** Standard citation and guidance shown on tickets and reports. */
  description: string;
};
